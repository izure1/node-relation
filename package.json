{
  "name": "node-relation",
  "version": "1.0.1",
  "description": "Manage strings that are related to each other.",
  "main": "dist/NodeRelation.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "izure <admin@izure.org>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "12.12.2",
    "typescript": "4.0.2"
  }
}
